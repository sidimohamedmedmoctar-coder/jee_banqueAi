# ── Test overrides ────────────────────────────────────────────────────────────
# Dummy values so env-var placeholders resolve without real credentials.
# These are NEVER used in production.

# JWT (kept identical to the dev value — already non-secret)
jwt.secret-key=9faSDFJKjkasdf09823jksdf90sdjkfsdf09823jksdfasdfasdf

# Telegram (bot not started in tests)
telegram.bot.token=test-dummy-token
telegram.bot.username=test-dummy-bot

# Spring AI OpenAI (no real HTTP calls in unit/slice tests)
spring.ai.openai.api-key=test-dummy-key

# H2 en mémoire pour les tests (isolation complète, jamais de fichier sur disque)
spring.datasource.url=jdbc:h2:mem:test-db;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
